{
  "name": "drawing-game-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end tests for the drawing game",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
